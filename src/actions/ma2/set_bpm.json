{
    "id": "ma2_set_bpm",
    "name": "grandMA2 - Change SpeedMaster BPM",
    "description": "Change SpeedMaster 1 BPM",
    "i18n": {
        "name": {
            "pt": "grandMA2 - Alterar BPM do SpeedMaster"
        },
        "description": {
            "pt": "Altera o BPM do SpeedMaster 1"
        }
    },
    "script_filter": " !(obj.type == 'trigger' && ((obj.when == 'displaying' && obj.item == 'any_song') || (obj.when == 'change' && obj.item == 'bpm'))) && h.isMinimumVersion('2.29.0') "
}
